/**
 * The show host dashboard shell — a fixed 2×2 grid of cards
 * (docs/stream-rework/show-cockpit-plan.md). Consumes four named slots
 * (identity, live, schedule, social); ShowDetailPage owns all data + handlers.
 */
export { default as GridShell } from './GridShell.vue';
