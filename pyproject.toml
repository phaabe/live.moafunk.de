[project]
name = "live-moafunk-de"
version = "0.1.0"
description = "Moafunk Radio - SoundCloud tracks data generator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["requests>=2.32.4"]

[tool.uv]
dev-dependencies = ["pytest>=7.4.0", "pytest-cov>=4.1.0"]
